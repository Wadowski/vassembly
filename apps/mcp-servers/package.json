{
  "name": "@vassembly/mcp-servers",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "up": "docker compose -f docker-compose.yml up -d --build",
    "down": "docker compose -f docker-compose.yml down",
    "logs": "docker compose -f docker-compose.yml logs -f"
  }
}
