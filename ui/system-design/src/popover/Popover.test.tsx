import { describe, expect, it } from 'vitest';
import { Popover } from './Popover';

describe('Popover', () => {
  it('exports component with displayName', () => {
    expect(Popover.displayName).toBe('Popover');
  });
});
