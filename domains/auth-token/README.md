# @vassembly/domain-auth-token

Auth token domain that handles JWT creation, verification, and decoding using the client-jwt package.

## Features

- **Create**: Generate JWT tokens with auth token data (role, userId, jwtId, expiresAt, issuedAt)
- **Verify**: Verify and decode JWT tokens with validation
- **Decode**: Decode JWT tokens without verification
