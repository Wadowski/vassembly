{
  "name": "@vassembly/client-mongodb",
  "version": "0.0.0",
  "main": "./src/index.ts", 
  "types": "./src/index.ts",
  "private": true,
  "scripts": {
    "lint": "eslint . --max-warnings 0",
    "check-types": "tsc --noEmit",
    "test": "vitest run",
    "dev": "docker-compose -p vassembly-e2e-mongo up -d && exec sleep infinity",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@types/node": "^22.19.7",
    "@vassembly/eslint-config": "workspace:*",
    "@vassembly/typescript-config": "workspace:*",
    "eslint": "^9.39.1",
    "typescript": "5.9.2",
    "vitest": "^2.0.0"
  },
  "dependencies": {
    "@vassembly/config": "workspace:^",
    "@vassembly/logger": "workspace:^",
    "@vassembly/model": "workspace:^",
    "mongodb": "^7.0.0"
  }
}
