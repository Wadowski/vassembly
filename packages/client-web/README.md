# @vassembly/client-web

Fetch-based web utilities for internal agent tools. No API keys or paid services.

- **Web search** — DuckDuckGo HTML Lite results parsing
- **Page content** — HTML fetch and text/media extraction

Used by `@vassembly/service-agent` internal tools `web-search` and `web-page-content`.
